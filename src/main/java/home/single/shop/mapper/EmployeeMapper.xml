<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="home.single.shop.mapper.EmployeeMapper">
	<!-- 직원 로그인 -->
	<select id="empLogin" parameterType="home.single.shop.vo.Employee" resultType="home.single.shop.vo.Employee">
		SELECT
			employee_id employeeId
			, employee_name employeeName
			, employee_level employeeLevel
		FROM employee
		WHERE employee_id = #{employeeId} AND employee_pw = PASSWORD(#{employeePw})
	</select>
</mapper>